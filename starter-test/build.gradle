dependencies {
    api "org.assertj:assertj-core:$assertjVersion"
    api "org.junit.jupiter:junit-jupiter:$jupiterVersion"

    api ("org.springframework.boot:spring-boot-starter-test") {
        exclude group: 'junit', module: 'junit'
    }
}